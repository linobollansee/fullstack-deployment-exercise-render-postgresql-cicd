# Database connection string for local development
# Replace with your actual PostgreSQL connection string
DATABASE_URL=postgresql://username:password@localhost:5432/quotes_db

# Example for Render.com PostgreSQL (external URL)
# DATABASE_URL=postgresql://user:password@host.render.com/database_name
